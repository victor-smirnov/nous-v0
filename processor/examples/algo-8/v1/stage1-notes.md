# Stage 1 notes — algo-8

Chunk word totals in parentheses (whitespace-separated words; punctuation not counted). Names are canonical from
target.txt / profile.txt; "short" marks a clipped re-naming of a target concept after its first full
introduction. Prior-knowledge and bucket items (a robot, making a sandwich, lunch, you try it, ...) are never
marked short. No new names coined. Part B: only chunk 5 says anything directly about a need — "Best part"
tells the child that what follows is the enjoyable bit, so "anyone can follow it without knowing why" installs
fun +0.2 (judgement call; nothing else in the text states a cost or gain to understanding, fun, doing it
myself, or safety).

1. (21) "brush your teeth" 3 → brushing your teeth; "you do things in order: wet the brush, add paste, brush, rinse. Steps, one after another" 16 → steps done one after another (first introduction).
2. (21) "a robot" + "It stares" 4 → a robot; "make a sandwich" 3 → making a sandwich; "You must say every step exactly: take bread, spread butter, add cheese" 12 → every step must be exact (first introduction).
3. (21) "the silly robot smears the whole tub" 6 → a silly robot; "the silly robot" 3 → a robot (overlap); "Say a bit of butter" 5 + "Exact steps" 2 = 7 → every step must be exact, short (the inexact instruction and its clipped re-name); "same steps: same result every time" 6 → same steps, same result every time (first introduction).
4. (24) "Steps one after another" 4 → steps done one after another, short; "must also end. Spread butter, spread butter, spread butter forever? No lunch, ever. There has to be a last step" 19 → it must come to an end (first introduction); "No lunch, ever" 3 → lunch (overlap).
5. (23) "every step is exact" 4 → every step must be exact, short; "anyone can follow it without knowing why. Your little brother could make it. Even the robot." 16 → anyone can follow it without knowing why (first introduction), path fun +0.2 from "Best part"; "the robot" 2 → a robot.
6. (21) "Exact steps" 2 → every step must be exact, short; "steps, one after another" 4 → steps done one after another, short; "that come to an end" 5 → it must come to an end, short; "Exact steps, one after another, that come to an end, have a name: an algorithm" 14 + "Your sandwich steps are an algorithm" 6 = 20 → an algorithm (first introduction; the definition spans the three properties); "sandwich steps" 2 → making a sandwich.
7. (22) "A computer" 2 → a computer; "that robot" 2 → a robot; "it follows without knowing why" 5 → anyone can follow it without knowing why, short; "A computer" 2 + "It only follows algorithms, exactly what the steps say, nothing more" 10 = 12 → a computer only follows algorithms (first introduction); "algorithms" 1 → an algorithm, short.
8. (23) "the algorithm" 2 → an algorithm, short; "same steps, same result every time" 6 → same steps, same result every time, short; "If one step in the algorithm is missing or mixed up" 11 + "the silly one" 3 + "That's a bug" 3 = 17 → a bug (first introduction). "the silly one" is not charged to a silly robot: it names the result, not the robot.
9. (23) "You try it" 3 + "pick something you do" 4 = 7 → you try it; "pick something you do" 4 + "Your own" 2 = 6 → doing it yourself (overlap); "Steps one after another" 4 → steps done one after another, short; "every step exact" 3 → every step must be exact, short; "it comes to an end" 5 → it must come to an end, short; "Your own algorithm" 3 → an algorithm, short.
