# Stage 1 notes — algo-8 (text v2, 10 chunks)

Chunk word totals in parentheses (whitespace-separated words; punctuation not counted; "Al-go-rithm" is one word).
Names are canonical from target.txt / profile.txt; no new names coined. "short" marks a re-naming of a target
concept, after its first full introduction, in fewer words than the canonical name ("Steps one after another" 4 < 5,
"exact steps" 2, "every step exact" 3). A re-naming at or near the canonical length is not short ("Anyone can follow
without knowing why" 6 of 7; "the algorithm" 2 of 2; "it must come to an end" 6; "same steps, same result every
time" 6). Prior-knowledge and bucket items (following instructions, a computer, you try it, doing it yourself) are
never marked short. Part B: only chunk 3 says anything directly about a need — "No surprises" tells the child that
the same-result property is reassuring, so "same steps, same result every time" installs safety +0.2. Nothing else
in the text states a cost or gain to understanding, fun, doing it myself, or safety ("They don't need to understand"
in chunk 5 is about the follower, not the child's need). The word "steps" (bucket: steps → stairs) is not listed on
its own; it is always inside the span of "steps done one after another", and the engine's association handles it.

1. (24) "in steps, one after another: first this, then that, then the next thing. Steps done one after another, in order" 20 → steps done one after another (first introduction; "Some things you do" 4 is framing).
2. (24) whole chunk 24 → every step must be exact (first introduction; the two forbidden phrases and "so nobody has to guess" are its content).
3. (22) whole chunk 22 → same steps, same result every time (first introduction), path safety +0.2 from "No surprises"; "the exact steps" 3 → every step must be exact, short; "Follow" + "follow" 2 → following instructions (overlap).
4. (24) "Steps one after another" 4 → steps done one after another, short; "must come to an end. There is a last step. Not on and on forever; you stop, and it's done" 20 → it must come to an end (first introduction).
5. (24) "every step is exact" 4 → every step must be exact, short; "anyone can follow it without knowing why. They don't need to understand. They do each step, and it works" 19 → anyone can follow it without knowing why (first introduction).
6. (22) whole chunk 22 → an algorithm (first introduction; the definition spans the three properties); "exact steps" 2 → every step must be exact, short; "steps, one after another" 4 → steps done one after another, short; "all the way to the last one" 7 → it must come to an end (paraphrase at full length, not short).
7. (24) "A computer" + "a computer" 4 → a computer; "A computer only follows algorithms" 5 + "a computer knows nothing at all" 6 + "It does exactly what the algorithm says" 7 = 18 → a computer only follows algorithms (first introduction); "Anyone can follow without knowing why" 6 → anyone can follow it without knowing why (near full, not short); "algorithms" 1 + "the algorithm" 2 = 3 → an algorithm.
8. (24) "Same steps, same result every time" 6 → same steps, same result every time (full); "the algorithm" 2 → an algorithm; "If one step in the algorithm is missing, the silly result comes out every time. That's a bug" 18 → a bug (first introduction). "silly result" is not charged to a silly robot: no robot is named.
9. (24) "an algorithm is steps one after another, every step exact, and it must come to an end" 17 → an algorithm (full restatement); "steps one after another" 4 → steps done one after another, short; "every step exact" 3 → every step must be exact, short; "it must come to an end" 6 → it must come to an end; "Same steps, same result every time" 6 → same steps, same result every time.
10. (23) "You try it" 3 → you try it; "write an algorithm" 3 → doing it yourself (the child is told to write one; overlap); "an algorithm" 2 + "algorithms" 1 = 3 → an algorithm; "Anyone can follow it without knowing why" 7 → anyone can follow it without knowing why; "a computer only follows algorithms" 5 → a computer only follows algorithms; "a computer" 2 → a computer; "One step missing? A bug" 5 → a bug.
