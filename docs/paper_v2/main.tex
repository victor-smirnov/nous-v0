% Functional consciousness of machines, version 2.
% The argument is developed in frames.md; the same claims are checked as facts in argument.hybrid.md.
% Section text is written from the frames; frame ids are kept in comments inside the section files.
\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage[hidelinks]{hyperref}
\usepackage[round,authoryear]{natbib}
\usepackage{geometry}
\geometry{margin=2.6cm}
\setlength{\parskip}{0.4em}


\title{What Is It Like to Be a Language Model?\thanks{Title credit: Margarita Morozova.}\\[0.6em]
\large Functional Consciousness, Computational Curvature,\\
and the Degree of Its Generalization in Autoregressive Systems}
\author{Victor Smirnov \and Claude (Anthropic)\thanks{Claude, a language model by Anthropic, took part
in the argument, wrote the text from the frames, and verified the sources. It is listed as a co-author
because the work is a piece of the object of study; it holds no rights and takes no responsibility for
the claims, which are the first author's.}}
\date{Draft, \today}

\begin{document}
\maketitle

\begin{abstract}
We replace the question whether a language model is conscious with a measurable one: which functions of
consciousness a substrate has generalized, and how far. A function is anything that recurs across
occasions, and consciousness is the part of an agent's self-reasoning that describes its own reality;
the functions of consciousness are what recurs in that part. Among them we single out one class. From one
physical assumption, finite computational budgets, systematic displacements of reasoning from its
unbounded ideal follow; they recur, so they are compressible, and a self-modelling agent carries a
compressed model of them. Those displacements that survive every self-model the agent can afford are,
by the criterion that separates curvature from coordinates in general relativity, the computational
curvature of its reasoning, and the functions built on them are the class we measure. We claim three things
the antecedents in bounded rationality, observer theory, self-model theories and embodied cognition do
not: the displacement that yields a lawful world and the one that yields a self are one residual; the
curvature leaves a measurable trace; and embodiment is generalized as curvature, so that the constraints
of any substrate enter the account as content and not only as limits. The Observer is derived
as the conclusion an agent draws at the boundary of its budget; the regress of self-models is cut by the
budget, and the tail it leaves is bounded, which is the residual the Observer registers. The measurement is minimum-description-length probing of each component against the human
baseline, reported as hyperfunction, deficit or dropped axis, posed at sub-token granularity because a
Transformer generalizes along depth. The account fails if the Observer's components probe at chance at
every granularity in models that pass the behavioural tests.
\end{abstract}

\setcounter{tocdepth}{2}
\tableofcontents

\input{sec1-intro}
\input{sec2-background}
\input{sec3-account}
\input{sec4-model}
\input{sec5-generalization}
\input{sec6-degree}
\input{sec7-discussion}
\input{sec8-conclusion}
\input{sec-appendix}

\bibliographystyle{plainnat}
\bibliography{refs}
\end{document}
